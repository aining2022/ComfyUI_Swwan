# rgthree 节点移植说明

本项目移植了 rgthree-comfy 的四个实用节点，用于增强 ComfyUI 的工作流控制能力。

## 节点列表

### 1. Fast Groups Muter
**类型**: 虚拟节点（纯前端）  
**功能**: 快速控制工作流中组（Group）内所有节点的静音状态

#### 特性
- 自动检测工作流中的所有组
- 为每个组提供独立的开关控制
- 支持按颜色过滤（如 "red,blue" 或 "#ff0000"）
- 支持按标题过滤（正则表达式）
- 多种排序方式：
  - position: 按位置排序（默认）
  - alphanumeric: 按字母顺序
  - custom alphabet: 自定义字母顺序
- 批量操作：全部静音、全部启用、全部切换
- 快速导航到组的位置

#### 属性配置
右键点击节点 -> Properties：
- `matchColors`: 颜色过滤
- `matchTitle`: 标题过滤（正则）
- `showNav`: 显示导航按钮
- `showAllGraphs`: 显示所有子图的组
- `sort`: 排序方式
- `customSortAlphabet`: 自定义排序字母表
- `toggleRestriction`: 切换限制（default/max one/always one）

#### 使用场景
- 快速启用/禁用工作流的整个部分
- 测试不同的工作流分支
- 使用组织织复杂工作流
- 通过隔离特定组进行调试

---

### 2. Fast Muter
**类型**: 虚拟节点（纯前端）  
**功能**: 快速控制连接节点的静音状态

#### 特性
- 自动检测所有连接的节点
- 为每个连接节点提供开关控制
- 支持穿透 Reroute 节点
- 批量操作：全部静音、全部启用、全部切换
- 切换限制选项

#### 属性配置
- `toggleRestriction`: 
  - default: 无限制
  - max one: 最多一个启用
  - always one: 始终保持一个启用

#### 使用场景
- 从单点控制多个节点
- 测试不同的处理路径
- 快速启用/禁用节点链

#### 连接方式
将需要控制的节点连接到 Fast Muter 的输入端，节点会自动显示对应的开关。

---

### 3. Image Comparer
**类型**: 服务器节点（带前端 UI）  
**功能**: 在 UI 中比较两张图片

#### 特性
- 两种比较模式：
  - Slide: 鼠标悬停滑动比较（默认）
  - Click: 点击切换图片
- 支持图片批次
- 自动从批次中选择图片
- 交互式图片选择器（批次 > 2 时）

#### 输入
- `image_a` (可选): 第一张图片
- `image_b` (可选): 第二张图片

**注意**: 
- 如果只提供一个输入且是批次，会自动选择前两张进行比较
- 如果两个输入都是批次，可以选择要比较的具体图片

#### 属性配置
- `comparer_mode`: 
  - Slide: 滑动模式
  - Click: 点击模式

#### 使用场景
- 比较处理前后的结果
- 评估不同模型的输出
- 质量控制和 A/B 测试
- 对比不同参数的效果

---

### 4. Seed (rgthree)
**类型**: 服务器节点（带前端增强）  
**功能**: 增强的种子节点，支持特殊功能

#### 特性
- 🎲 随机种子生成
- 🔄 种子递增/递减
- 📌 固定随机种子
- ♻️ 上次使用的种子追踪
- 📊 可选的种子值显示
- 服务器端随机生成回退

#### 特殊种子值
- `-1`: 每次随机（前端处理）
- `-2`: 递增（基于上次种子）
- `-3`: 递减（基于上次种子）

#### 按钮功能
- **🎲 Randomize Each Time**: 设置为每次随机模式
- **🎲 New Fixed Random**: 生成新的固定随机种子
- **♻️ Use Last Queued Seed**: 使用上次队列中的种子

#### 属性配置
- `randomMax`: 随机种子最大值（默认: 1125899906842624）
- `randomMin`: 随机种子最小值（默认: 0）
- `showLastSeed`: 显示/隐藏上次种子值（右键菜单）

#### 使用场景
- 一致的可重现结果
- 系统化的种子探索
- 快速随机化测试
- 种子值管理和追踪

#### 工作流示例
```
[Seed (rgthree)] → [KSampler] → [Save Image]
```

---

## 文件结构

```
ComfyUI_Swwan/
├── fast_groups_muter.py       # Fast Groups Muter 后端
├── fast_muter.py              # Fast Muter 后端
├── image_comparer.py          # Image Comparer 后端
├── seed.py                    # Seed 后端
├── web/js/
│   ├── fast_groups_muter.js   # Fast Groups Muter 前端
│   ├── fast_groups_muter_entry.js  # 简化版
│   ├── muter.js               # Fast Muter 前端
│   ├── image_comparer.js      # Image Comparer 前端
│   ├── seed.js                # Seed 前端
│   ├── base_node.js           # 基础节点类
│   ├── base_any_input_connected_node.js
│   ├── base_node_mode_changer.js
│   ├── utils.js, utils_canvas.js, utils_widgets.js
│   ├── constants.js, rgthree.js
│   ├── common/                # 共享组件
│   └── services/              # 服务模块
└── __init__.py                # 节点注册
```

## 安装说明

1. 将此仓库克隆到 ComfyUI 的 `custom_nodes` 目录
2. 重启 ComfyUI
3. 节点会出现在 "rgthree" 分类下

## 依赖说明

这些节点复用了 rgthree-comfy 的前端代码，包括：
- 基础节点类和工具函数
- Canvas 绘制工具
- Widget 组件
- 服务模块

所有依赖文件已包含在 `web/js/` 目录中。

## 注意事项

### 虚拟节点
Fast Groups Muter 和 Fast Muter 是虚拟节点，不参与实际的工作流执行，仅用于 UI 控制。

### 组的概念
Fast Groups Muter 需要在 ComfyUI 中先创建组（Group）才能使用：
1. 选择多个节点
2. 右键 -> "Add Group"
3. 设置组的标题和颜色

### 静音 vs 旁路
- **静音 (Mute/Never)**: 节点完全不执行
- **旁路 (Bypass)**: 节点被跳过，但数据会传递

这些节点控制的是"静音"状态。

## 原始来源

- 项目: [rgthree-comfy](https://github.com/rgthree/rgthree-comfy)
- 作者: rgthree
- 许可: MIT License

## 相关节点

rgthree-comfy 还有其他实用节点可以移植：
- Fast Groups Bypasser: 控制组的旁路状态
- Fast Bypasser: 控制节点的旁路状态
- Context/Context Switch: 上下文管理
- Power Prompt: 增强的提示词节点
- 等等...

## 问题反馈

如有问题或建议，请在 GitHub 上提交 Issue。
